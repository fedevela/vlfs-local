GEMINI_API_KEY=your_gemini_api_key_here
VLFS_ROOT_DIR=/absolute/path/to/agent_os_memory/memories

# Set to "true" to use the local 'gemini' CLI app for text generation and the local 'ollama' API for vector embeddings.
LOCAL_DEV_MODE=false
